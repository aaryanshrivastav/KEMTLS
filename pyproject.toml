[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "kemtls-rust-ext"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
manifest-path = "rust/kemtls_core/Cargo.toml"
module-name = "kemtls_core"
python-source = "src"
